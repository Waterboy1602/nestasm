export enum FileType {
  SVG = "svg",
  JSON = "json",
}

export enum Status {
  IDLE = "idle",
  START = "start",
  PROCESSING = "processing",
  FINISHED = "finished",
  ERROR = "error",
}

export enum OptimizationAlgo {
  LBF = "lbf",
  SPARROW = "sparrow",
}
